flush ();
return type :  void
content :  ใช้ล้างข้อมูลใน stream ( คือล้างข้อมูลในไฟล์ ) และกำหนดให้ buffer ทำงาน ตามลำดับ
( buffer ทำงาน คือ เขียนข้อมูลลงไฟล์ )

PrintWriter ();
คือ object หนึ่งที่เอาไว้เก็บหรือเขียนข้อมูล

BufferedReader();
คือตัวที่จะไล่อ่านทั้งหมดและเก็บไว้ในตัวเอง และเราสามารถเรียกอ่านแต่ละบรรทัด

InputStreamReader();
คือตัวที่รับค่า